/* Class representing the Segment object. A Segment is one square of the Snake. */

export class Segment {
    startX;
    startY;

    constructor(startX, startY) {
        this.startX = startX;
        this.startY = startY;
    }

    render() {

    }
}