/* Enum-like variable representing possible directions the Snake can travel. */
const Direction = {
    UP: 1,
    DOWN: 2,
    LEFT: 3,
    RIGHT: 4,
    NONE: 5 // Only happens at the start of the game before a key is pressed.
}